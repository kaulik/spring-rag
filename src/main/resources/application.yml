spring:
  application:
    name: spring-rag
  config:
    import: "optional:configserver:${CONFIG_SERVER_URL:http://host.docker.internal:8686}"
  cloud:
    config:
      fail-fast: false
      retry:
        max-attempts: 6
        initial-interval: 2000
        max-interval: 3000
